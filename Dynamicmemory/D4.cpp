// macros variable;
#include<bits/stdc++.h>
using namespace std;

#define PI 3.14
// the above statement shows that the value of pi will be 3.24 when it will used at the places.

#define min(a, b) (((a) < (b)) ? (a) : (b))
// the above statement is a kind of if else statement for chekcing for the maximum number.(ternary operator  )
int main(){
    int r=10;
    double area=PI * r * r;
    cout<<"Area is: "<<area<<endl;

return 0;
}